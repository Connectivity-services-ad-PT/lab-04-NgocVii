# .env.example
# IoT Ingestion Service Environment Variables

# Server configuration
HOST=0.0.0.0
PORT=8000

# Authentication (for local development)
AUTH_TOKEN=local-dev-token

# Rate limiting
RATE_LIMIT_PER_MINUTE=60

# Log level
LOG_LEVEL=info